<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="genesip.com.ej.insuarance_agent_mgt_diary">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Agent_dashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".client.Client_reg_main"></activity>
        <activity android:name=".appointment.Appointment_dashboard" />
        <activity android:name=".payments.PayementDashboard" />
        <activity android:name=".AgentLogin"></activity>
        <activity android:name=".AgentRegistration">

        </activity>
    </application>

</manifest>